# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Payment API
    version: 0.0.1
paths:
    payment/order:
        post:
            tags:
                - Payment
            operationId: Payment_PaymentOrder
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PaymentOrderRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentOrderReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    payment/update:
        post:
            tags:
                - Payment
            operationId: Payment_UpdatePayment
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdatePaymentRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdatePaymentReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    reconciliation/create:
        post:
            tags:
                - Payment
            operationId: Payment_CreatedReconciliation
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreatedReconciliationRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreatedReconciliationReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    reconciliation/list:
        get:
            tags:
                - Payment
            operationId: Payment_ListReconciliation
            parameters:
                - name: PlayTime
                  in: query
                  schema:
                    type: string
                - name: EndTime
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListReconciliationReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreatedReconciliationReply:
            type: object
            properties:
                ReconciliationId:
                    type: string
        CreatedReconciliationRequest:
            type: object
            properties:
                CreatedTime:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListReconciliationReply:
            type: object
            properties:
                list:
                    type: array
                    items:
                        $ref: '#/components/schemas/ReconciliationItem'
        PaymentOrderReply:
            type: object
            properties:
                Url:
                    type: string
        PaymentOrderRequest:
            type: object
            properties:
                OrderSn:
                    type: string
        ReconciliationItem:
            type: object
            properties:
                TaskName:
                    type: string
                ReconciliationNo:
                    type: string
                ReconciliationDate:
                    type: string
                Amount:
                    type: number
                    format: float
                ActualAmount:
                    type: number
                    format: float
                Proportion:
                    type: string
                HandlerStatus:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdatePaymentReply:
            type: object
            properties:
                Result:
                    type: string
        UpdatePaymentRequest:
            type: object
            properties: {}
tags:
    - name: Payment
