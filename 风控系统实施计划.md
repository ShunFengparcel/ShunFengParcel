# 顺丰包裹支付系统 - 风控系统实施计划

## 📋 项目概述
本文档为顺丰包裹支付系统的风控体系建设规划，包含风控规则引擎、交易监控服务、安全审计日志和异常交易检测四大核心模块。

---

## 🎯 总体目标
- **风险防控**: 实时监控和拦截异常交易
- **合规审计**: 完整记录所有交易和操作日志
- **智能决策**: 基于规则引擎的自动化风控决策
- **数据洞察**: 通过报表系统提供风险分析能力

---

## 📊 实施阶段规划

### 阶段一：基础设施搭建 (第1-2周)

#### 1.1 数据库设计
- [ ] 风控规则表 (risk_rules)
- [ ] 交易监控记录表 (transaction_monitor)
- [ ] 安全审计日志表 (security_audit_log)
- [ ] 异常交易表 (abnormal_transactions)
- [ ] 风险黑名单表 (risk_blacklist)
- [ ] 风险等级配置表 (risk_level_config)

#### 1.2 项目结构规划
```
ShunFengParcel/
├── internal/
│   ├── service/
│   │   ├── payment.go (现有)
│   │   ├── risk_control.go (新增-风控服务)
│   │   ├── monitor.go (新增-监控服务)
│   │   └── audit.go (新增-审计服务)
│   ├── biz/
│   │   ├── risk_engine.go (新增-规则引擎业务逻辑)
│   │   ├── risk_detector.go (新增-异常检测逻辑)
│   │   └── risk_report.go (新增-报表生成逻辑)
│   └── data/
│       ├── risk_rule.go (新增-规则数据层)
│       ├── audit_log.go (新增-日志数据层)
│       └── monitor_data.go (新增-监控数据层)
├── config/
│   ├── risk_rule.go (新增-规则模型)
│   ├── audit_log.go (新增-审计日志模型)
│   └── monitor_record.go (新增-监控记录模型)
├── api/
│   └── helloworld/
│       └── riskcontrol/
│           └── riskcontrol.proto (新增-风控服务API定义)
└── utils/
    ├── risk_calculator.go (新增-风险计算工具)
    └── rule_matcher.go (新增-规则匹配工具)
```

---

### 阶段二：风控规则引擎 (第3-4周)

#### 2.1 核心功能
- **规则配置管理**
  - [ ] 规则增删改查接口
  - [ ] 规则优先级管理
  - [ ] 规则启用/禁用控制
  - [ ] 规则版本管理

- **规则类型**
  - [ ] 金额限制规则（单笔/日累计/月累计）
  - [ ] 频率限制规则（时间窗口内交易次数）
  - [ ] 地域限制规则（IP地理位置、收发件地址）
  - [ ] 用户行为规则（新用户限制、异常行为检测）
  - [ ] 黑名单规则（用户、手机号、地址）

- **规则引擎架构**
  ```
  请求 → 规则加载 → 规则匹配 → 风险评分 → 决策执行 → 结果返回
  ```

#### 2.2 技术实现要点
- 使用责任链模式实现规则链
- Redis缓存热门规则，提升性能
- 支持规则动态加载，无需重启服务
- 规则执行结果记录到审计日志

#### 2.3 数据模型
```sql
CREATE TABLE `risk_rules` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `rule_code` varchar(50) NOT NULL COMMENT '规则编码',
  `rule_name` varchar(100) NOT NULL COMMENT '规则名称',
  `rule_type` enum('amount','frequency','geo','behavior','blacklist') NOT NULL,
  `rule_config` json NOT NULL COMMENT '规则配置(JSON格式)',
  `risk_score` int DEFAULT '0' COMMENT '风险分数',
  `priority` int DEFAULT '0' COMMENT '优先级',
  `status` enum('active','inactive') DEFAULT 'active',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_rule_code` (`rule_code`)
) COMMENT='风控规则表';
```

#### 2.4 API接口设计
- `POST /api/v1/risk/rule/create` - 创建规则
- `PUT /api/v1/risk/rule/update` - 更新规则
- `DELETE /api/v1/risk/rule/delete/{id}` - 删除规则
- `GET /api/v1/risk/rule/list` - 规则列表
- `POST /api/v1/risk/evaluate` - 风险评估接口

---

### 阶段三：交易监控服务 (第5-6周)

#### 3.1 监控维度
- **实时监控**
  - [ ] 交易金额异常监控
  - [ ] 交易频率异常监控
  - [ ] 支付渠道异常监控
  - [ ] 用户行为异常监控

- **指标监控**
  - [ ] 支付成功率
  - [ ] 平均交易金额
  - [ ] 订单量趋势
  - [ ] 异常交易占比

- **告警机制**
  - [ ] 阈值告警（金额、频率超限）
  - [ ] 规则触发告警
  - [ ] 系统异常告警
  - [ ] 多渠道通知（邮件、短信、企业微信）

#### 3.2 监控流程
```
交易发起 → 前置检查 → 实时监控 → 风险评估 → 
   ↓
[正常] → 继续交易流程
[可疑] → 人工审核队列
[高危] → 自动拦截 + 告警
```

#### 3.3 数据模型
```sql
CREATE TABLE `transaction_monitor` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `monitor_no` varchar(50) NOT NULL COMMENT '监控编号',
  `order_no` varchar(20) NOT NULL COMMENT '订单号',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `transaction_amount` decimal(10,2) NOT NULL COMMENT '交易金额',
  `monitor_type` varchar(50) NOT NULL COMMENT '监控类型',
  `risk_level` enum('low','medium','high','critical') DEFAULT 'low',
  `risk_score` int DEFAULT '0' COMMENT '风险分数',
  `triggered_rules` json COMMENT '触发的规则列表',
  `monitor_result` enum('pass','review','block') DEFAULT 'pass',
  `handle_status` enum('pending','processing','handled') DEFAULT 'pending',
  `handle_result` varchar(200) COMMENT '处理结果',
  `handler_id` bigint COMMENT '处理人ID',
  `ip_address` varchar(50) COMMENT 'IP地址',
  `device_info` json COMMENT '设备信息',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_risk_level` (`risk_level`),
  KEY `idx_created_at` (`created_at`)
) COMMENT='交易监控记录表';
```

#### 3.4 集成点
- 在 `payment.go` 的 `PaymentOrder` 方法中集成监控
- 在 `UpdatePayment` 回调中记录监控结果
- 定时任务扫描异常订单

---

### 阶段四：安全审计日志 (第7-8周)

#### 4.1 日志类型
- **操作日志**
  - [ ] 用户登录/登出
  - [ ] 订单创建/修改/取消
  - [ ] 支付操作
  - [ ] 退款操作
  - [ ] 管理员操作

- **系统日志**
  - [ ] API调用记录
  - [ ] 数据库操作记录
  - [ ] 第三方接口调用（支付宝等）
  - [ ] 异常错误日志

- **安全日志**
  - [ ] 风控规则触发记录
  - [ ] 异常访问记录
  - [ ] 权限变更记录
  - [ ] 敏感操作记录

#### 4.2 日志标准
- **完整性**: 记录操作前后状态
- **可追溯**: 包含用户、时间、IP、设备等信息
- **不可篡改**: 日志写入后不可修改
- **合规性**: 满足金融行业审计要求

#### 4.3 数据模型
```sql
CREATE TABLE `security_audit_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `log_no` varchar(50) NOT NULL COMMENT '日志编号',
  `log_type` enum('operation','system','security') NOT NULL,
  `log_level` enum('info','warning','error','critical') DEFAULT 'info',
  `module` varchar(50) NOT NULL COMMENT '模块名称',
  `action` varchar(100) NOT NULL COMMENT '操作动作',
  `user_id` bigint COMMENT '操作用户ID',
  `user_name` varchar(50) COMMENT '操作用户名',
  `ip_address` varchar(50) COMMENT 'IP地址',
  `user_agent` varchar(500) COMMENT '用户代理',
  `request_method` varchar(10) COMMENT '请求方法',
  `request_url` varchar(500) COMMENT '请求URL',
  `request_params` json COMMENT '请求参数',
  `response_status` int COMMENT '响应状态码',
  `response_data` json COMMENT '响应数据',
  `before_data` json COMMENT '操作前数据',
  `after_data` json COMMENT '操作后数据',
  `execution_time` int COMMENT '执行时间(ms)',
  `error_message` text COMMENT '错误信息',
  `trace_id` varchar(50) COMMENT '追踪ID',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_log_type` (`log_type`),
  KEY `idx_created_at` (`created_at`),
  KEY `idx_trace_id` (`trace_id`)
) COMMENT='安全审计日志表';
```

#### 4.4 实现方式
- 使用 Kratos 中间件实现全局日志拦截
- 异步写入日志，避免影响主流程性能
- 日志分级存储（热数据/冷数据）
- 定期归档历史日志

#### 4.5 日志查询接口
- `GET /api/v1/audit/logs` - 查询审计日志
- `GET /api/v1/audit/logs/{id}` - 查看日志详情
- `GET /api/v1/audit/export` - 导出审计日志
- `GET /api/v1/audit/statistics` - 日志统计分析

---

### 阶段五：异常交易检测 (第9-10周)

#### 5.1 检测策略
- **基于规则的检测**
  - [ ] 单笔金额异常（超过历史平均值N倍）
  - [ ] 交易频率异常（短时间内多笔交易）
  - [ ] 交易时间异常（非常规时间段交易）
  - [ ] 地域异常（IP地址与常用地址不符）
  - [ ] 新用户异常（新注册即大额交易）

- **基于统计的检测**
  - [ ] 用户交易画像建模
  - [ ] 偏离度计算（与历史行为对比）
  - [ ] 同业对比（与同类用户对比）
  - [ ] 趋势分析（交易模式突变）

- **基于机器学习的检测（可选，后期扩展）**
  - [ ] 异常检测算法（Isolation Forest）
  - [ ] 聚类分析（识别异常群体）
  - [ ] 时序预测（预测异常交易）

#### 5.2 检测流程
```
实时交易数据 → 特征提取 → 规则匹配 → 统计分析 → 
   ↓
综合评分 → 风险等级判定 → 处理策略
   ↓
[低风险] → 正常放行
[中风险] → 加强监控 + 记录
[高风险] → 人工审核
[极高风险] → 自动拦截 + 告警
```

#### 5.3 数据模型
```sql
CREATE TABLE `abnormal_transactions` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `abnormal_no` varchar(50) NOT NULL COMMENT '异常编号',
  `order_no` varchar(20) NOT NULL COMMENT '订单号',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `abnormal_type` varchar(50) NOT NULL COMMENT '异常类型',
  `abnormal_reason` text COMMENT '异常原因',
  `risk_level` enum('low','medium','high','critical') DEFAULT 'medium',
  `risk_score` int DEFAULT '0' COMMENT '风险分数',
  `detection_time` datetime NOT NULL COMMENT '检测时间',
  `transaction_amount` decimal(10,2) COMMENT '交易金额',
  `transaction_count` int COMMENT '交易次数',
  `user_behavior_score` int COMMENT '用户行为评分',
  `geo_risk_score` int COMMENT '地域风险评分',
  `device_risk_score` int COMMENT '设备风险评分',
  `handle_status` enum('pending','reviewing','handled','ignored') DEFAULT 'pending',
  `handle_result` varchar(500) COMMENT '处理结果',
  `handler_id` bigint COMMENT '处理人ID',
  `handled_at` datetime COMMENT '处理时间',
  `evidence_data` json COMMENT '证据数据',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_abnormal_no` (`abnormal_no`),
  KEY `idx_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_risk_level` (`risk_level`),
  KEY `idx_handle_status` (`handle_status`),
  KEY `idx_detection_time` (`detection_time`)
) COMMENT='异常交易表';
```

#### 5.4 异常交易报表
**日报表字段：**
- 日期
- 异常交易总数
- 按风险等级分布（低/中/高/极高）
- 按异常类型分布（金额/频率/地域/行为）
- 处理状态分布（待处理/审核中/已处理/已忽略）
- Top 10 高风险用户
- Top 10 异常交易类型
- 误报率统计
- 拦截成功率

**周报表/月报表额外字段：**
- 趋势分析图表
- 同比/环比数据
- 风险热力图
- 用户风险分层统计
- 规则有效性分析

---

### 阶段六：报表系统开发 (第11-12周)

#### 6.1 报表类型
- **实时监控大屏**
  - [ ] 当日交易监控概览
  - [ ] 实时风险告警
  - [ ] 异常交易趋势图
  - [ ] 风控规则触发排行

- **定期报表**
  - [ ] 异常交易日报
  - [ ] 异常交易周报
  - [ ] 异常交易月报
  - [ ] 风控效果分析报表

- **专项报表**
  - [ ] 用户风险画像报表
  - [ ] 规则有效性分析报表
  - [ ] 审计日志分析报表
  - [ ] 黑名单分析报表

#### 6.2 报表生成机制
- 定时任务（使用cron表达式配置）
- 手动触发生成
- 异步生成，支持大数据量
- 多格式导出（PDF、Excel、HTML）

#### 6.3 数据模型
```sql
CREATE TABLE `risk_reports` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `report_no` varchar(50) NOT NULL COMMENT '报表编号',
  `report_type` enum('daily','weekly','monthly','custom') NOT NULL,
  `report_name` varchar(100) NOT NULL COMMENT '报表名称',
  `start_date` date NOT NULL COMMENT '统计开始日期',
  `end_date` date NOT NULL COMMENT '统计结束日期',
  `total_transactions` bigint COMMENT '总交易数',
  `abnormal_count` bigint COMMENT '异常交易数',
  `abnormal_rate` decimal(5,2) COMMENT '异常率(%)',
  `total_amount` decimal(15,2) COMMENT '总交易金额',
  `abnormal_amount` decimal(15,2) COMMENT '异常交易金额',
  `blocked_count` int COMMENT '拦截数量',
  `review_count` int COMMENT '待审核数量',
  `false_positive_count` int COMMENT '误报数量',
  `false_positive_rate` decimal(5,2) COMMENT '误报率(%)',
  `risk_level_distribution` json COMMENT '风险等级分布',
  `abnormal_type_distribution` json COMMENT '异常类型分布',
  `top_risk_users` json COMMENT 'Top风险用户',
  `top_rules_triggered` json COMMENT 'Top触发规则',
  `report_data` json COMMENT '完整报表数据',
  `generated_by` bigint COMMENT '生成人',
  `status` enum('generating','completed','failed') DEFAULT 'generating',
  `file_path` varchar(500) COMMENT '文件路径',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_report_no` (`report_no`),
  KEY `idx_report_type` (`report_type`),
  KEY `idx_start_date` (`start_date`),
  KEY `idx_created_at` (`created_at`)
) COMMENT='风控报表表';
```

#### 6.4 报表API接口
- `POST /api/v1/report/generate` - 生成报表
- `GET /api/v1/report/list` - 报表列表
- `GET /api/v1/report/{id}` - 查看报表
- `GET /api/v1/report/{id}/download` - 下载报表
- `GET /api/v1/report/dashboard` - 监控大屏数据

---

## 📈 核心指标与KPI

### 风控效果指标
- **准确率**: 正确识别异常交易的比例 ≥ 95%
- **召回率**: 实际异常交易中被识别出的比例 ≥ 90%
- **误报率**: 正常交易被误判为异常的比例 ≤ 5%
- **响应时间**: 风险评估接口响应时间 ≤ 100ms
- **拦截率**: 高风险交易自动拦截比例 = 100%

### 系统性能指标
- **并发支持**: 支持 1000 TPS
- **日志写入**: 支持 10000 条/秒
- **报表生成**: 日报 ≤ 5分钟，月报 ≤ 30分钟
- **可用性**: 系统可用性 ≥ 99.9%

---

## 🔧 技术选型

### 基础框架
- **Go Kratos**: 微服务框架
- **GORM**: ORM框架
- **MySQL**: 主数据库
- **Redis**: 缓存 + 规则热加载

### 监控告警
- **Prometheus**: 指标采集
- **Grafana**: 监控大屏
- **AlertManager**: 告警管理

### 日志系统
- **Zap**: 结构化日志（项目已使用）
- **Elasticsearch**: 日志存储与检索（可选）
- **Kibana**: 日志分析（可选）

### 任务调度
- **Cron**: 定时任务调度
- **RabbitMQ**: 异步任务队列（项目已集成）

---

## 🚀 部署计划

### 开发环境
- Week 1-2: 开发环境搭建完成
- Week 3-10: 功能开发 + 单元测试

### 测试环境
- Week 11: 集成测试
- Week 12: 性能测试 + 压力测试

### 预生产环境
- Week 13: 灰度发布 10% 流量
- Week 14: 灰度发布 50% 流量

### 生产环境
- Week 15: 全量发布

---

## ✅ 交付物清单

### 代码交付
- [ ] 风控规则引擎代码
- [ ] 交易监控服务代码
- [ ] 安全审计日志代码
- [ ] 异常交易检测代码
- [ ] 报表系统代码
- [ ] 单元测试代码（覆盖率 ≥ 80%）

### 文档交付
- [ ] 需求文档
- [ ] 设计文档（架构设计、数据库设计、接口设计）
- [ ] 开发文档
- [ ] 测试文档（测试用例、测试报告）
- [ ] 部署文档
- [ ] 运维手册
- [ ] 用户手册

### 数据交付
- [ ] 数据库建表脚本
- [ ] 初始化数据脚本
- [ ] 数据迁移脚本（如需要）

---

## 🎓 团队能力要求

### 开发人员
- 熟悉 Go 语言及 Kratos 框架
- 了解风控业务逻辑
- 熟悉 MySQL、Redis
- 了解分布式系统设计

### 测试人员
- 熟悉自动化测试
- 了解性能测试工具
- 熟悉风控业务场景

### 运维人员
- 熟悉 Docker、K8s
- 熟悉监控告警系统
- 了解数据库运维

---

## ⚠️ 风险与应对

### 技术风险
**风险**: 规则引擎性能不足，影响交易效率
**应对**: 
- 规则缓存到 Redis
- 异步执行非阻塞规则
- 限流保护

### 业务风险
**风险**: 误报率过高，影响用户体验
**应对**:
- 规则灰度发布
- 建立规则评估机制
- 支持人工复核
- 白名单机制

### 数据风险
**风险**: 日志数据量过大，存储成本高
**应对**:
- 日志分级存储
- 定期归档冷数据
- 使用对象存储

---

## 📞 联系与支持

### 项目负责人
- **风控规则引擎**: [待指定]
- **交易监控服务**: [待指定]
- **安全审计日志**: [待指定]
- **异常交易检测**: [待指定]

### 技术支持
- **开发组**: [待指定]
- **测试组**: [待指定]
- **运维组**: [待指定]

---

## 📝 变更记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0 | 2025-10-28 | AI Assistant | 初始版本 |

---

## 🎯 下一步行动

### 立即执行
1. ✅ **评审本计划** - 组织技术评审会议
2. ⏳ **确认资源** - 确认开发人员、测试人员配备
3. ⏳ **环境准备** - 准备开发/测试环境
4. ⏳ **数据库设计评审** - DBA 参与数据库设计评审

### 本周内完成
5. ⏳ **创建数据库表** - 执行建表脚本
6. ⏳ **搭建项目骨架** - 创建基础代码结构
7. ⏳ **制定详细开发计划** - 细化每个功能点的开发任务

### 两周内完成
8. ⏳ **完成风控规则引擎原型** - 可演示的最小可行产品
9. ⏳ **完成监控服务基础框架** - 基本的监控数据收集能力
10. ⏳ **完成审计日志中间件** - 集成到现有支付流程

---

**备注**: 本计划为初步规划，具体实施过程中可根据实际情况调整。建议每周进行进度评审，及时识别和解决问题。


