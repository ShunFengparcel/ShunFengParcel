# 用户认证和授权说明

## 问题描述

之前的代码中，当用户未登录或未提供 user_id 时，系统会使用固定的测试用户 ID=1。这导致所有未登录用户看到的都是同一个用户的数据。

## 解决方案

### 1. 后端改进

#### 创建认证中间件
文件：`server/ShunFengParcel/ShunFengParcel/internal/middleware/auth.go`

- 从请求头中提取 JWT token
- 解析 token 获取用户ID
- 将用户ID存入 context

#### 更新订单服务
文件：`server/ShunFengParcel/ShunFengParcel/internal/service/order_service.go`

**之前的逻辑：**
```go
userId := req.UserId
if userId == 0 {
    userId = 1 // 使用固定的测试用户ID
}
```

**现在的逻辑：**
```go
// 1. 优先使用请求中的 user_id
userId := req.UserId

// 2. 如果没有，从 token 中获取
if userId == 0 {
    if ctxUserID, ok := ctx.Value("user_id").(int64); ok {
        userId = ctxUserID
    } else {
        // 3. 如果都没有，返回401错误
        return &pb.ListOrdersReply{
            Code:    401,
            Message: "请先登录",
        }, nil
    }
}
```

### 2. 前端改进

#### 请求拦截器
文件：`web/user-client/utils/request.js`

自动在所有请求头中添加 token：
```javascript
const token = uni.getStorageSync('token')
if (token) {
  config.header = {
    ...config.header,
    'Authorization': `Bearer ${token}`
  }
}
```

#### 订单Store
文件：`web/user-client/stores/order.js`

- 检查用户是否登录
- 使用真实的用户ID
- 处理401错误，自动跳转登录页

## 工作流程

### 1. 用户登录
```
用户 → 微信登录 → 后端验证 → 生成JWT token → 返回给前端
前端 → 保存token到本地存储 → 保存用户信息
```

### 2. 访问需要认证的接口
```
前端 → 从本地存储读取token → 添加到请求头
后端 → 从请求头提取token → 解析获取用户ID → 查询该用户的数据
```

### 3. Token过期或无效
```
后端 → 返回401错误
前端 → 检测到401 → 提示用户 → 跳转到登录页
```

## JWT Token 结构

```json
{
  "user_id": 8,
  "openid": "test_openid_xxx",
  "sub": "8",
  "exp": 1762218721,
  "iat": 1761613921
}
```

- `user_id`: 用户ID
- `openid`: 微信openid
- `exp`: 过期时间（7天后）
- `iat`: 签发时间

## 测试步骤

### 1. 测试登录
```bash
curl -X POST http://localhost:18000/user/wxlogin \
  -H "Content-Type: application/json" \
  -d '{
    "code": "test_user_001",
    "nickname": "测试用户1",
    "avatar_url": "https://example.com/avatar1.jpg"
  }'
```

响应：
```json
{
  "code": "200",
  "message": "登录成功",
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "openid": "test_openid_test_user_001",
  "nickname": "测试用户1",
  "userId": 9
}
```

### 2. 使用token访问订单列表
```bash
curl -X GET "http://localhost:18000/api/v1/orders?page=1&page_size=10" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..."
```

### 3. 不带token访问（应该返回401）
```bash
curl -X GET "http://localhost:18000/api/v1/orders?page=1&page_size=10"
```

响应：
```json
{
  "code": "401",
  "message": "请先登录"
}
```

## 数据隔离

现在每个用户只能看到自己的数据：

- **用户A** (ID=1) 登录 → 只能看到自己的订单
- **用户B** (ID=2) 登录 → 只能看到自己的订单
- **未登录用户** → 无法访问订单列表，会被要求登录

## 安全性

### Token安全
- Token有效期：7天
- 使用HMAC-SHA256签名
- Secret密钥应该从环境变量读取（生产环境）

### 数据安全
- 用户只能访问自己的数据
- 所有需要认证的接口都会验证token
- Token过期后需要重新登录

## 下一步优化

### 1. 添加中间件到HTTP服务器
文件：`server/ShunFengParcel/ShunFengParcel/internal/server/http.go`

```go
import "ShunFengParcel/internal/middleware"

func NewHTTPServer(...) *http.Server {
    // ...
    srv := http.NewServer(
        http.Middleware(
            middleware.AuthMiddleware(), // 添加认证中间件
            // ... 其他中间件
        ),
    )
    // ...
}
```

### 2. 刷新Token机制
- 当token快过期时，自动刷新
- 避免用户频繁重新登录

### 3. 退出登录
- 清除本地token
- 可选：将token加入黑名单（Redis）

## 重新编译和测试

### 1. 重新编译后端
```bash
cd server/ShunFengParcel/ShunFengParcel
go build -o bin/server.exe ./cmd/ShunFengParcel
./bin/server.exe -conf configs
```

### 2. 重新编译前端
```bash
cd web/user-client
npm run dev:mp-weixin
```

### 3. 测试流程
1. 清除小程序缓存
2. 重新登录
3. 查看订单列表（应该只显示当前登录用户的订单）
4. 退出登录
5. 尝试访问订单列表（应该跳转到登录页）

现在系统已经实现了真正的用户隔离，每个用户只能看到自己的数据！🎉
