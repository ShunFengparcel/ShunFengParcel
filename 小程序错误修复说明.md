# 小程序错误修复说明

## 错误信息
```
TypeError: Cannot read property '$vm' of undefined
at getAppVm (vendor.js? [sm]:7424)
```

## 问题原因
这个错误是因为在页面组件初始化时调用了 `getApp()`，但此时 App 实例可能还没有完全初始化完成。

## 已修复的文件
1. `web/user-client/App.vue` - 在 onLaunch 中初始化 globalData
2. `web/user-client/pages/send/create.vue` - 添加 try-catch 保护
3. `web/user-client/pages/send/address.vue` - 添加 try-catch 保护

## 重新编译步骤

### 1. 停止当前的编译进程
在终端中按 `Ctrl+C` 停止 `npm run dev:mp-weixin`

### 2. 重新编译
```bash
cd web/user-client
npm run dev:mp-weixin
```

### 3. 在微信开发者工具中刷新
- 点击微信开发者工具的"编译"按钮
- 或者按快捷键 `Ctrl+B` (Windows) / `Cmd+B` (Mac)

## 测试步骤

### 1. 测试登录
1. 打开登录页面
2. 勾选用户协议
3. 点击"微信一键登录"
4. 查看是否能正常跳转到首页

### 2. 测试创建订单
1. 点击底部 Tab "寄快递"
2. 点击"快速寄件"
3. 选择寄件地址和收件地址
4. 输入重量
5. 查看是否能正常显示价格

### 3. 测试查快递
1. 点击底部 Tab "查快递"
2. 查看订单列表是否正常显示

## 如果问题仍然存在

### 方案1：清除缓存
1. 在微信开发者工具中，点击菜单：工具 -> 清除缓存
2. 选择"清除全部缓存"
3. 重新编译项目

### 方案2：删除 node_modules 重新安装
```bash
cd web/user-client
rm -rf node_modules
rm -rf dist
npm install
npm run dev:mp-weixin
```

### 方案3：检查 uni-app 版本
确保使用的是最新版本的 uni-app：
```bash
cd web/user-client
npm update
```

## 预防措施

在使用 `getApp()` 时，始终添加保护：

```javascript
// ❌ 不推荐
const app = getApp()
app.globalData.xxx = value

// ✅ 推荐
try {
  const app = getApp()
  if (app) {
    app.globalData = app.globalData || {}
    app.globalData.xxx = value
  }
} catch (error) {
  console.error('操作失败:', error)
}
```

## 相关文档
- uni-app 生命周期：https://uniapp.dcloud.net.cn/tutorial/page.html#lifecycle
- 微信小程序 getApp：https://developers.weixin.qq.com/miniprogram/dev/reference/api/getApp.html
