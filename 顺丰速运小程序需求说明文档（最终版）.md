# 顺丰速运小程序需求说明文档（最终版）

**版本：** V2.0  
**日期：** 2023年10月27日  
**文档状态：** 最终版  

---

## 1. 文档概述

### 1.1 项目背景与目标
**背景：** 随着电商业务发展和用户对快递服务要求提高，需要构建一个集用户服务、快递员作业、后台运营于一体的智能化快递服务平台。

**核心目标：**
- 用户端：寄件流程≤3分钟完成，物流信息实时推送，异常处理及时率>95%
- 快递员端：任务调度准确率>98%，收派件效率提升30%
- 运营端：全流程数据可视化，异常订单自动识别率>90%

### 1.2 项目范围
**包含模块：**
- 用户服务全流程（寄件、查件、支付、售后）
- 快递员作业全流程（接单、收派、结算）
- 后台运营全流程（调度、监控、财务、客服）
- 数据智能分析系统

**不包含：**
- 国际快递报关系统
- 仓储管理系统
- 车辆路径规划系统

---

## 2. 系统架构总览

### 2.1 技术架构
```
表现层：微信小程序(用户端) + APP(快递员端) + Web管理系统(运营端)
应用层：用户服务 | 订单服务 | 调度服务 | 物流服务 | 支付服务 | 消息服务
数据层：用户DB | 订单DB | 物流DB | 财务DB | 地理信息DB
基础设施：云计算平台 | 对象存储 | CDN | 短信/推送服务
```

### 2.2 业务架构
```
用户域：注册登录、地址管理、订单管理、支付账户
订单域：订单创建、状态管理、费用计算、服务选择
物流域：轨迹跟踪、节点管理、异常处理
运营域：智能调度、绩效管理、财务管理、客户服务
```

---

## 3. 用户端详细需求

### 3.1 首页模块

#### 3.1.1 运单查询
**功能描述：** 提供快捷运单查询入口
```
页面元素：
- 搜索框：占位符"输入运单号查询"
- 扫码按钮：右侧扫码图标
- 历史查询：下拉展示最近5个查询记录

交互流程：
1. 用户输入运单号（支持手动输入/粘贴）
2. 点击搜索或键盘"搜索"
3. 系统验证运单号格式（12位数字）
   - 格式正确：跳转物流详情页
   - 格式错误：红色提示"运单号格式不正确"

异常处理：
- 网络异常：显示"网络连接失败，请重试"
- 运单不存在：显示"运单号不存在，请核对"
- 系统繁忙：显示"系统繁忙，请稍后再试"
```

#### 3.1.2 快捷寄件
**功能描述：** 基于用户历史行为快速发起寄件
```
业务规则：
- 读取用户最近3个寄件地址作为快捷选项
- 自动填充常用服务类型（基于历史选择）
- 预估费用基于最近一次重量计算

数据字段：
{
  "default_sender": AddressObject,
  "common_receivers": [AddressObject],
  "preferred_service": String,
  "estimated_fee": Decimal
}
```

### 3.2 寄件流程模块

#### 3.2.1 地址管理
**地址录入流程：**
```
步骤1：选择地址类型
  - 选项：寄件地址 / 收件地址
  - 默认：寄件地址

步骤2：填写地址信息
  必填字段：
    * 联系人姓名（长度2-20字符，支持中文/英文）
    * 手机号码（11位，格式验证）
    * 所在地区（省市区三级联动选择）
    * 详细地址（5-120字符，包含楼栋门牌）
  
  选填字段：
    * 公司名称
    * 地址标签（家/公司/学校/其他）
    * 设为默认地址

步骤3：地址验证
  - 调用高德地图API进行地址标准化
  - 验证服务范围（电子围栏检查）
  - 通过：保存成功
  - 不通过：提示"该地址暂不支持服务"
```

#### 3.2.2 服务选择
**服务选项矩阵：**
| 服务类型     | 时效   | 价格系数 | 重量限制 | 特殊要求    |
| ------------ | ------ | -------- | -------- | ----------- |
| 顺丰即日     | 当日达 | 2.0x     | 20kg     | 18:00前取件 |
| 顺丰特快     | 次日达 | 1.5x     | 30kg     | 无          |
| 顺丰标快     | 隔日达 | 1.0x     | 50kg     | 无          |
| 顺丰快递包裹 | 3-5天  | 0.7x     | 100kg    | 无          |

**保价规则：**
```
- 基础保价费：1元
- 费率：声明价值的0.5%
- 最低保价：1元
- 最高保价：20000元
- 计算公式：max(1, 声明价值 * 0.005)
```

#### 3.2.3 费用计算引擎
**计费规则：**
```
首重价格（1kg） + 续重价格 * ceil(重量 - 1)

示例：标快服务，重量2.3kg
首重：12元
续重：2元/kg
费用：12 + 2 * ceil(2.3 - 1) = 12 + 2 * 2 = 16元

特殊规则：
- 轻抛货：体积重量 = 长(cm)*宽(cm)*高(cm)/6000
- 实际计费重量 = max(实际重量, 体积重量)
```

### 3.3 订单管理模块

#### 3.3.1 订单状态机
```
待接单 --(快递员接单)--> 已接单 --(上门取件)--> 已揽收
已揽收 --(中转扫描)--> 运输中 --(到达网点)--> 派送中
派送中 --(签收)--> 已签收
派送中 --(异常)--> 派件异常 --(处理)--> 已签收/已退回

取消路径：
待接单 --(用户取消)--> 已取消
已接单 --(双方同意取消)--> 已取消
```

#### 3.3.2 物流详情页
**信息架构：**
```
头部信息区：
- 运单号（复制按钮）
- 当前状态（大号字体+状态图标）
- 预计送达时间（动态更新）

地图轨迹区：
- 包裹当前位置标记
- 运输路径动画
- 关键节点标记（揽收、中转、派送）

时间轴区：
- 节点时间线（倒序排列）
- 每个节点包含：时间、地点、操作描述
- 最新节点高亮显示

操作按钮区：
- 联系快递员（直接拨打）
- 分享物流（生成分享卡片）
- 投诉建议（跳转客服）
```

---

## 4. 快递员端详细需求

### 4.1 任务调度系统

#### 4.1.1 智能派单算法
**输入参数：**
```javascript
{
  "order": {
    "address": GeoPoint,
    "time_window": DateTimeRange,
    "service_type": String,
    "weight": Decimal
  },
  "couriers": [
    {
      "id": String,
      "location": GeoPoint,
      "workload": Integer,
      "skills": Array,
      "performance_score": Decimal
    }
  ]
}
```

**派单逻辑：**
```python
def assign_order(order, couriers):
    # 第一步：筛选符合条件的快递员
    candidates = filter_couriers_by_availability(couriers)
    
    # 第二步：计算综合得分
    for courier in candidates:
        distance_score = calculate_distance_score(order.address, courier.location)
        workload_score = calculate_workload_score(courier.workload)
        performance_score = courier.performance_score
        time_score = calculate_time_score(order.time_window, courier.schedule)
        
        total_score = (
            distance_score * 0.4 +
            workload_score * 0.25 +
            performance_score * 0.25 +
            time_score * 0.1
        )
        courier.assignment_score = total_score
    
    # 第三步：选择最优快递员
    best_courier = max(candidates, key=lambda x: x.assignment_score)
    return best_courier.id
```

#### 4.1.2 任务优先级管理
**优先级规则：**
```
P0（最高）：超时订单、客户投诉订单
P1（高）：预约时间即将到期的订单
P2（中）：普通已接单订单
P3（低）：新订单、柔性时间订单
```

### 4.2 收派件作业流程

#### 4.2.1 上门取件标准流程
```
步骤1：确认订单信息
  - 扫描寄件码或输入运单号
  - 系统显示：收寄件信息、服务类型、特殊要求
  - 核对物品类型是否与描述一致

步骤2：称重计费
  - 连接蓝牙秤获取重量（精确到0.1kg）
  - 系统自动计算运费
  - 如与预估差异>10%，需用户确认
  - 拍照留存（贵重物品）

步骤3：收取运费
  - 寄付：用户小程序支付/现金支付
  - 到付：标记为到付订单
  - 月结：验证月结账号有效性

步骤4：打包揽收
  - 使用标准包装材料
  - 粘贴运单（条码清晰可见）
  - 扫描运单条码完成揽收
  - 系统自动发送揽收通知
```

#### 4.2.2 异常情况处理手册
**场景1：用户取消订单（已接单）**
```
处理流程：
1. 快递员收到取消请求
2. 判断是否已出发：
   - 未出发：直接同意取消
   - 已出发：与用户协商空跑费（0-10元）
3. 系统记录取消原因
4. 订单状态更新为"已取消"
```

**场景2：地址错误/无法联系**
```
处理流程：
1. 首次联系失败：等待30分钟重试
2. 三次联系失败：标记为"联系不上"
3. 系统自动通知寄件人
4. 24小时内无响应：订单挂起
5. 客服介入处理
```

### 4.3 绩效与结算

#### 4.3.1 绩效计算规则
```javascript
// 收件绩效
pickup_score = base_pickup_fee + (weight * weight_factor) + premium_service_bonus

// 派件绩效  
delivery_score = base_delivery_fee + time_bonus + quality_bonus

// 综合评分
total_score = (pickup_score + delivery_score) * performance_multiplier

// 绩效乘数基于：
// - 准时率（40%权重）
// - 客户评分（30%权重）  
// - 投诉率（30%权重）
```

#### 4.3.2 结算周期
```
日结：在线支付订单，T+1结算
周结：现金收款，每周二结算上月款项
月结：企业月结客户，次月15日结算
```

---

## 5. 后台运营端详细需求

### 5.1 智能调度中心

#### 5.1.1 实时监控大屏
**数据指标：**
```
核心指标：
- 实时订单量：当前待处理订单数
- 接单率：过去1小时接单比例
- 超时率：预计时间超时订单比例
- 资源利用率：快递员负载情况

地理分布：
- 订单热力图（实时更新）
- 快递员位置分布
- 运力缺口区域预警
```

#### 5.1.2 人工调度干预
**干预场景：**
```
1. 系统派单不合理
   - 手动重新分配订单
   - 指定特定快递员
   
2. 紧急订单处理
   - 手动提升优先级
   - 指定经验丰富快递员
   
3. 异常情况处理
   - 快递员请假/离职
   - 恶劣天气影响
   - 系统故障恢复
```

### 5.2 客户服务系统

#### 5.2.1 工单分类与处理
**工单类型矩阵：**
| 类型     | SLA   | 处理团队 | 升级条件     |
| -------- | ----- | -------- | ------------ |
| 物流咨询 | 2小时 | 一线客服 | 无           |
| 时效投诉 | 1小时 | 投诉专员 | 4小时未解决  |
| 物品损坏 | 4小时 | 理赔专员 | 24小时未解决 |
| 服务态度 | 4小时 | 质控团队 | 涉及重大投诉 |

#### 5.2.2 智能客服知识库
**知识结构：**
```
常见问题FAQ（自动匹配）
- 物流状态解释
- 费用计算规则
- 服务范围查询
- 投诉建议渠道

业务规则库
- 禁寄物品列表
- 保价理赔流程
- 费用争议处理
- 异常情况处理
```

### 5.3 财务管理模块

#### 5.3.1 对账流程
```
日对账流程：
1. 系统自动对账（04:00执行）
2. 差异检测：
   - 支付状态不一致
   - 金额不匹配
   - 重复支付
3. 异常处理：
   - 自动修复可识别差异
   - 人工审核复杂情况
4. 生成对账报告
```

#### 5.3.2 财务报表
**日报表内容：**
```
收入汇总：
- 按支付方式：微信/现金/月结
- 按服务类型：即日/特快/标快
- 按区域：各营业部收入分布

成本分析：
- 快递员薪酬成本
- 包装材料成本
- 运输中转成本
- 系统运营成本

利润计算：
- 毛利润 = 总收入 - 直接成本
- 净利润 = 毛利润 - 间接成本
```

---

## 6. 数据模型详细设计

### 6.1 核心数据表结构

#### 6.1.1 订单表 (sf_orders)
```sql
CREATE TABLE sf_orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(20) UNIQUE NOT NULL COMMENT '订单号',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    courier_id BIGINT COMMENT '快递员ID',
    
    -- 寄件信息
    sender_name VARCHAR(50) NOT NULL,
    sender_phone VARCHAR(20) NOT NULL,
    sender_address JSON NOT NULL COMMENT '详细地址信息',
    
    -- 收件信息  
    receiver_name VARCHAR(50) NOT NULL,
    receiver_phone VARCHAR(20) NOT NULL,
    receiver_address JSON NOT NULL,
    
    -- 服务信息
    service_type ENUM('same_day', 'next_day', 'standard', 'economy') NOT NULL,
    product_type VARCHAR(50) COMMENT '物品类型',
    is_insured BOOLEAN DEFAULT FALSE,
    insured_value DECIMAL(10,2) COMMENT '保价金额',
    
    -- 费用信息
    estimated_weight DECIMAL(8,3) COMMENT '预估重量',
    actual_weight DECIMAL(8,3) COMMENT '实际重量',
    estimated_fee DECIMAL(10,2) COMMENT '预估费用',
    actual_fee DECIMAL(10,2) COMMENT '实际费用',
    payment_method ENUM('sender_pay', 'receiver_pay', 'monthly') NOT NULL,
    payment_status ENUM('pending', 'paid', 'failed', 'refunded') NOT NULL,
    
    -- 时间信息
    expected_pickup_time DATETIME COMMENT '期望上门时间',
    actual_pickup_time DATETIME COMMENT '实际上门时间',
    expected_delivery_time DATETIME COMMENT '期望送达时间',
    actual_delivery_time DATETIME COMMENT '实际送达时间',
    
    -- 状态信息
    order_status ENUM('pending', 'accepted', 'picked_up', 'in_transit', 'out_for_delivery', 'delivered', 'cancelled', 'exception') NOT NULL,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_user_id (user_id),
    INDEX idx_courier_id (courier_id),
    INDEX idx_order_status (order_status),
    INDEX idx_created_at (created_at)
) COMMENT '订单主表';
```

#### 6.1.2 物流轨迹表 (sf_tracking_logs)
```sql
CREATE TABLE sf_tracking_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    tracking_no VARCHAR(20) NOT NULL COMMENT '运单号',
    
    -- 节点信息
    scan_time DATETIME NOT NULL COMMENT '扫描时间',
    location_code VARCHAR(20) NOT NULL COMMENT '网点编码',
    location_name VARCHAR(100) NOT NULL COMMENT '网点名称',
    scan_type ENUM('pickup', 'arrival', 'departure', 'delivery', 'exception') NOT NULL,
    
    -- 详细信息
    description VARCHAR(200) NOT NULL COMMENT '节点描述',
    operator_id VARCHAR(50) COMMENT '操作员ID',
    device_id VARCHAR(50) COMMENT '设备ID',
    
    -- 地理信息
    longitude DECIMAL(10,6) COMMENT '经度',
    latitude DECIMAL(10,6) COMMENT '纬度',
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_order_id (order_id),
    INDEX idx_tracking_no (tracking_no),
    INDEX idx_scan_time (scan_time),
    INDEX idx_location_code (location_code)
) COMMENT '物流轨迹表';
```

### 6.2 API接口设计

#### 6.2.1 创建订单接口
```javascript
POST /api/v1/orders

请求体：
{
  "sender": {
    "name": "张三",
    "phone": "13800138000",
    "address": {
      "province": "广东省",
      "city": "深圳市",
      "district": "南山区",
      "detail": "科技园南区1001号"
    }
  },
  "receiver": {
    "name": "李四", 
    "phone": "13900139000",
    "address": {
      "province": "北京市",
      "city": "北京市",
      "district": "朝阳区",
      "detail": "建国门外大街1号"
    }
  },
  "service": {
    "type": "next_day",
    "product_type": "documents",
    "is_insured": true,
    "insured_value": 1000
  },
  "payment": {
    "method": "sender_pay"
  },
  "schedule": {
    "expected_pickup_time": "2023-10-28 14:00:00"
  }
}

响应体：
{
  "code": 200,
  "data": {
    "order_no": "SF20231027123456",
    "estimated_fee": 23.00,
    "pickup_code": "123456",
    "status": "pending"
  }
}
```

---

## 7. 非功能性需求

### 7.1 性能指标
```
响应时间：
- 页面加载时间：< 2秒
- API响应时间：< 500ms（P95）
- 数据库查询：< 100ms

吞吐量：
- 支持峰值QPS：10,000
- 并发用户数：100,000
- 日订单处理能力：500万

可用性：
- 系统可用性：99.99%
- 数据可靠性：99.999%
- 故障恢复时间：< 5分钟
```

### 7.2 安全要求
```
数据安全：
- 敏感数据加密存储（AES-256）
- 传输层加密（TLS 1.3）
- 访问令牌有效期2小时

业务安全：
- 防重复提交（5秒内同一请求拒绝）
- 防刷单机制（同一用户日限10单）
- 金额一致性校验（前后端双重验证）

隐私保护：
- 用户数据脱敏处理
- 日志中屏蔽敏感信息
- 符合GDPR/个人信息保护法
```

### 7.3 监控告警
**监控指标：**
```
业务监控：
- 订单创建成功率
- 支付成功率  
- 接单超时率
- 客户投诉率

系统监控：
- CPU/Memory使用率
- 数据库连接数
- API错误率
- 响应时间分布

告警规则：
- 错误率 > 1% 持续5分钟
- 响应时间 > 2秒 持续10分钟
- 订单积压 > 1000 持续30分钟
```

---

## 8. 部署与运维

### 8.1 基础设施要求
```
计算资源：
- 应用服务器：8核16G * 10台
- 数据库服务器：16核64G * 3台（主从）
- 缓存服务器：8核16G * 3台（Redis集群）

网络要求：
- 带宽：100Mbps 独享
- CDN：全国节点覆盖
- 防火墙：WAF防护

存储要求：  
- 数据库：SSD存储，1TB
- 文件存储：对象存储，10TB
- 备份存储：冷备份，50TB
```

### 8.2 灾备方案
```
同城双活：
- 两个机房同时提供服务
- 数据实时同步
- 故障自动切换

异地备份：
- 异地机房数据备份
- RPO < 5分钟
- RTO < 30分钟
```

---

## 9. 项目里程碑

### 9.1 开发阶段
```
第一阶段（6周）：核心框架
  - 用户注册登录
  - 基础寄件流程
  - 订单管理

第二阶段（8周）：完整功能  
  - 支付系统集成
  - 物流跟踪
  - 快递员APP

第三阶段（6周）：高级功能
  - 智能调度
  - 数据分析
  - 优化体验
```

### 9.2 测试计划
```
单元测试：代码覆盖率 > 80%
集成测试：全流程端到端测试
性能测试：负载测试、压力测试
安全测试：渗透测试、漏洞扫描
用户验收测试：真实用户场景测试
```

---

## 10. 风险控制

### 10.1 技术风险
```
数据库性能风险：
-  mitigation：分库分表方案预备
-  mitigation：读写分离架构

第三方服务依赖风险：
-  mitigation：多供应商备选
-  mitigation：降级方案设计
```

### 10.2 业务风险
```
订单高峰风险：
-  mitigation：弹性扩缩容机制
-  mitigation：排队限流策略

资金安全风险：
-  mitigation：多重对账机制
-  mitigation：人工审核流程
```

---

**文档审批：**
- 产品经理：______________
- 技术负责人：______________  
- 项目经理：______________
- 业务方代表：______________

**文档版本历史：**
| 版本 | 日期       | 修改内容             | 修改人 |
| ---- | ---------- | -------------------- | ------ |
| V1.0 | 2023-10-20 | 初稿                 | 张三   |
| V1.1 | 2023-10-25 | 细化业务流程         | 李四   |
| V2.0 | 2023-10-27 | 最终版，补充技术细节 | 王五   |