# 顺丰速运小程序企业级开发文档

## 🎯 项目启动指南

### 1. 技术栈选择理由
- **Kratos**: 微服务治理、高性能、云原生
- **GVA**: 快速开发运营后台、RBAC权限管理
- **Uniapp**: 跨端开发、生态丰富
- **PostgreSQL**: 事务强一致性、JSON支持
- **Redis**: 高性能缓存、分布式锁
- **Elasticsearch**: 全文搜索、日志分析

---

## 🏗️ 微服务拆分方案

### 核心服务划分（建议8个核心服务）

#### 1. **用户服务 (user-service)**
```go
// 职责边界
- 用户注册/登录/鉴权
- 地址管理
- 用户信息管理
- 会员体系
```

#### 2. **订单服务 (order-service)**
```go
// 职责边界  
- 订单创建/状态管理
- 费用计算引擎
- 订单查询/搜索
- 订单统计
```

#### 3. **物流服务 (logistics-service)**
```go
// 职责边界
- 物流轨迹记录
- 运单号生成
- 节点状态更新
- 预计时间计算
```

#### 4. **配送服务 (dispatch-service)**
```go
// 职责边界
- 智能调度算法
- 快递员管理
- 任务分配
- 绩效计算
```

#### 5. **支付服务 (payment-service)**
```go
// 职责边界
- 支付流程管理
- 微信支付集成
- 对账系统
- 退款处理
```

#### 6. **消息服务 (notification-service)**
```go
// 职责边界
- 消息模板管理
- 多渠道推送
- 实时消息
- 通知记录
```

#### 7. **商品服务 (product-service)**
```go
// 职责边界
- 服务产品管理
- 价格策略
- 库存管理
- 商品分类
```

#### 8. **运营服务 (operation-service)**
```go
// 职责边界
- 数据统计
- 报表生成
- 运营配置
- 监控告警
```

---

## 🔧 开发环境搭建

### 1. 开发工具链
```bash
# 必须安装
- Go 1.19+
- Node.js 16+
- Docker & Docker Compose
- Git

# 推荐工具
- Goland/VSCode
- Apifox/Postman
- RedisInsight
- pgAdmin
```

### 2. 本地开发环境
```yaml
# docker-compose.dev.yml
version: '3.8'
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: sf_express
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes

  elasticsearch:
    image: elasticsearch:8.5.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    ports:
      - "9200:9200"

  jaeger:
    image: jaegertracing/all-in-one:1.35
    ports:
      - "16686:16686"
      - "4317:4317"

volumes:
  postgres_data:
```

---

## 🛠️ 项目结构规范

### 1. 微服务标准目录结构
```
user-service/
├── cmd/
│   └── server/
│       ├── main.go
│       └── wire.go
├── internal/
│   ├── biz/           # 业务逻辑层
│   │   ├── user.go
│   │   └── address.go
│   ├── data/          # 数据访问层
│   │   ├── user.go
│   │   └── redis.go
│   ├── service/       # 服务实现层
│   │   └── user.go
│   └── conf/          # 配置定义
├── api/               # API定义
│   └── user/v1/
│       ├── user.pb.go
│       └── user_http.pb.go
├── configs/           # 配置文件
├── pkg/               # 公共包
└── Dockerfile
```

### 2. 公共包设计 (pkg目录)
```go
// pkg/ 目录结构
pkg/
├── app/              // 应用启动
├── config/           // 配置管理
├── database/         // 数据库连接
├── redis/           // Redis客户端
├── logger/          // 日志组件
├── middleware/      // 中间件
├── util/           // 工具函数
├── constant/       // 常量定义
└── errorx/         // 错误处理
```

---

## 🔒 中间件设计

### 1. 必须实现的中间件

#### 认证中间件
```go
func JwtAuth() middleware.Middleware {
    return func(handler middleware.Handler) middleware.Handler {
        return func(ctx context.Context, req interface{}) (interface{}, error) {
            // JWT令牌验证
            // 用户权限检查
            return handler(ctx, req)
        }
    }
}
```

#### 限流中间件
```go
func RateLimit() middleware.Middleware {
    return func(handler middleware.Handler) middleware.Handler {
        return func(ctx context.Context, req interface{}) (interface{}, error) {
            // Redis分布式限流
            // 滑动窗口算法
            return handler(ctx, req)
        }
    }
}
```

#### 日志中间件
```go
func Logging() middleware.Middleware {
    return func(handler middleware.Handler) middleware.Handler {
        return func(ctx context.Context, req interface{}) (interface{}, error) {
            start := time.Now()
            resp, err := handler(ctx, req)
            
            // 结构化日志记录
            log.Info("request completed",
                "method", extractMethod(ctx),
                "duration", time.Since(start),
                "error", err)
            
            return resp, err
        }
    }
}
```

#### 链路追踪中间件
```go
func Tracing() middleware.Middleware {
    return func(handler middleware.Handler) middleware.Handler {
        return func(ctx context.Context, req interface{}) (interface{}, error) {
            span := otel.Tracer("").Start(ctx, "handler")
            defer span.End()
            
            ctx = trace.ContextWithSpan(ctx, span)
            return handler(ctx, req)
        }
    }
}
```

#### 熔断器中间件
```go
func CircuitBreaker() middleware.Middleware {
    cb := gobreaker.NewCircuitBreaker(gobreaker.Settings{})
    
    return func(handler middleware.Handler) middleware.Handler {
        return func(ctx context.Context, req interface{}) (interface{}, error) {
            return cb.Execute(func() (interface{}, error) {
                return handler(ctx, req)
            })
        }
    }
}
```

---

## 📊 数据库设计要点

### 1. PostgreSQL表设计规范
```sql
-- 统一表结构模板
CREATE TABLE table_name (
    id BIGSERIAL PRIMARY KEY,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at TIMESTAMPTZ,
    
    -- 业务字段
    column_name data_type NOT NULL,
    
    -- 索引
    INDEX idx_name (column_name),
    
    -- 约束
    CONSTRAINT uk_name UNIQUE (column_name)
) WITH (OIDS = FALSE);

-- 启用Row Level Security
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;
```

### 2. 关键表设计
```sql
-- 订单表分区设计（按月份分区）
CREATE TABLE orders_202401 PARTITION OF orders 
    FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');

-- JSONB字段使用（存储动态数据）
CREATE TABLE orders (
    id BIGSERIAL PRIMARY KEY,
    order_data JSONB NOT NULL,
    sender_info JSONB NOT NULL,
    receiver_info JSONB NOT NULL,
    
    -- GIN索引支持JSON查询
    INDEX idx_order_data_gin ON orders USING GIN (order_data)
);
```

---

## 🚀 性能优化策略

### 1. 10万并发架构设计

#### 缓存策略
```go
type CacheStrategy struct {
    // 本地缓存 (1级)
    LocalCache *ristretto.Cache
    // Redis缓存 (2级)  
    RedisCache *redis.Client
    // 数据库 (3级)
    DB *gorm.DB
}

func (c *CacheStrategy) GetUser(ctx context.Context, id int64) (*User, error) {
    // 1. 查本地缓存
    // 2. 查Redis
    // 3. 查数据库并回填缓存
}
```

#### 数据库连接池配置
```go
func InitDB() *gorm.DB {
    db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{
        PrepareStmt: true, // 预编译
    })
    
    sqlDB, _ := db.DB()
    sqlDB.SetMaxIdleConns(20)      // 最大空闲连接
    sqlDB.SetMaxOpenConns(100)     // 最大打开连接  
    sqlDB.SetConnMaxLifetime(time.Hour)
    
    return db
}
```

#### Redis集群配置
```go
func InitRedis() *redis.ClusterClient {
    return redis.NewClusterClient(&redis.ClusterOptions{
        Addrs: []string{
            "redis1:6379",
            "redis2:6379", 
            "redis3:6379",
        },
        PoolSize: 100, // 连接池大小
    })
}
```

---

## 🔄 开发流程建议

### 1. 第一周：基础设施搭建
**目标：** 完成技术框架和基础组件

```bash
# Day 1-2: 项目脚手架
- 创建所有微服务项目结构
- 配置Docker开发环境
- 搭建CI/CD流水线

# Day 3-4: 数据库和缓存
- 设计核心表结构
- 实现数据库连接池
- 配置Redis集群

# Day 5-7: 基础中间件
- 实现认证、限流、日志中间件
- 配置链路追踪
- 搭建监控系统
```

### 2. 第二周：核心业务开发
**目标：** 完成用户、订单、物流核心流程

```bash
# 并行开发三个核心服务
- 用户服务：注册登录、地址管理
- 订单服务：创建订单、状态机、费用计算
- 物流服务：轨迹记录、节点管理
```

### 3. 第三周：完整业务闭环
**目标：** 完成支付、配送、消息服务

```bash
# 完成剩余服务开发
- 支付服务：微信支付集成
- 配送服务：智能调度算法  
- 消息服务：实时通知推送
```

### 4. 第四周：系统优化和测试
**目标：** 性能优化和压力测试

```bash
# 系统优化
- 数据库查询优化
- 缓存策略调优
- 接口性能测试

# 压力测试
- 10万并发压力测试
- 稳定性测试
- 安全漏洞扫描
```

---

## 🛡️ 企业级标准实现

### 1. 安全防护
```go
// 数据加密
func EncryptSensitiveData(data string) (string, error) {
    // 使用AES-256加密敏感数据
}

// SQL注入防护
func SafeQuery(db *gorm.DB, query string, args ...interface{}) *gorm.DB {
    // 使用参数化查询
    return db.Where(query, args...)
}

// XSS防护
func XSSFilter(input string) string {
    // HTML标签转义
    return html.EscapeString(input)
}
```

### 2. 监控告警体系
```yaml
# prometheus.yml 配置
scrape_configs:
  - job_name: 'sf-express'
    static_configs:
      - targets: ['user-service:8000', 'order-service:8001']
    metrics_path: /metrics
```

### 3. 日志规范
```go
// 结构化日志
logger.Info("user login success",
    "user_id", userID,
    "ip", clientIP,
    "user_agent", userAgent,
    "timestamp", time.Now())

// 错误日志分级
logger.Error("database connection failed",
    "error", err,
    "database", "user_db",
    "operation", "get_user")
```

---

## 🚨 常见坑点预警

### 1. 分布式事务问题
```go
// 使用Saga模式解决分布式事务
func CreateOrderSaga(ctx context.Context, req *CreateOrderRequest) error {
    // 1. 预创建订单
    // 2. 扣减库存
    // 3. 生成物流单
    // 4. 任何失败则触发补偿操作
}
```

### 2. 缓存一致性
```go
// 缓存更新策略
func UpdateUserCache(user *User) error {
    // 先更新数据库，再删除缓存
    // 使用双删策略防止缓存穿透
}
```

### 3. 接口幂等性
```go
// 防止重复提交
func IdempotentCreateOrder(ctx context.Context, req *CreateOrderRequest) (*Order, error) {
    // 基于请求ID实现幂等
    idempotentKey := fmt.Sprintf("order:%s", req.RequestId)
    
    // 使用Redis分布式锁
    lock, err := redis.SetNX(idempotentKey, "1", 5*time.Minute)
    if !lock {
        return nil, errors.New("request duplicated")
    }
}
```

---

## 📋 开发检查清单

### 每日开发前检查
- [ ] 代码从最新main分支拉取
- [ ] 数据库迁移脚本已执行
- [ ] 依赖服务(Redis/PostgreSQL)正常运行
- [ ] 单元测试通过

### 代码提交前检查  
- [ ] 代码格式化 (go fmt)
- [ ] 静态检查 (go vet)
- [ ] 单元测试覆盖率 >80%
- [ ] API文档更新

### 部署前检查
- [ ] 压力测试通过
- [ ] 安全扫描通过
- [ ] 性能基准测试
- [ ] 回滚方案准备

---

## 🎯 第一步行动指南

### 立即开始（今天就能完成）

1. **环境准备** (1小时)
   ```bash
   # 安装必备工具
   brew install go node docker
   
   # 克隆项目模板
   git clone https://github.com/go-kratos/kratos-layout sf-express
   ```

2. **本地开发环境** (2小时)
   ```bash
   # 启动基础设施
   docker-compose -f docker-compose.dev.yml up -d
   
   # 验证服务
   curl http://localhost:9200  # Elasticsearch
   curl http://localhost:16686 # Jaeger
   ```

3. **创建第一个服务** (3小时)
   ```bash
   # 创建用户服务
   kratos new user-service
   
   # 实现第一个API
   # 用户注册接口
   ```

4. **编写第一个测试** (1小时)
   ```go
   func TestUserRegister(t *testing.T) {
       // 单元测试
       // 集成测试
   }
   ```

这个开发文档为你提供了完整的企业级开发指导。按照这个规划，你可以有条不紊地开始开发，避免常见的坑点，确保项目质量和性能。